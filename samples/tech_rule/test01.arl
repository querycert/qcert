import java.lang.*;
import java.util.*;
import rete.*;
import tests.entities.*;

ruleset test01 {
  rule R01 {
    when {
      c:Customer(age == 32.0);
    } then {
      System.out.println("Customer =" + c.name);
    }
  }
}
