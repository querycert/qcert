/* set union yields unsorted array; breaks compare? */
module main {
  wasm22(x) return y {
    let even := [0.0, 2.0, 4.0];
    let odd := [1.0, 3.0];
    let all0 := Runtime.max(even, odd);
    let all1 := Runtime.max(odd, even);
    y := [all0, all1, Runtime.equal(all0, all1)];
    /* imp returns false */
    /* js runtime seems to return true */
  }
}
